# Launch requirements and accounts

This checklist is the completion contract for the public oracle. Software tests, a running website and registered domains do not satisfy external publisher or data requirements.

| Requirement | Current evidence | Next action |
| --- | --- | --- |
| Four domains | Purchased for $78.06; DNS, HTTPS, both sites and canonical redirects verified | Assign renewal owner; automatic renewal is off |
| Public open source repository | Public elizaOS/blackwell-index; Linux, container and Pyth protocol checks passed for the deployed follow-up release | Continue exact-revision verification on every change |
| Real source collection | Public Oracle all-four, Azure GB200 and Verda B200/B300/GB300 retrievals verified | Continue scheduled collection; inspect coverage, source errors and sustained availability |
| AWS credentials | Not supplied | Read-only `pricing:GetProducts` principal and keys; optional session token. Also obtain `ec2:DescribeInstanceTypes` access to verify GB300 hardware before enabling normalization |
| Google credentials and SKU map | Not supplied | Enable Cloud Billing API; restrict a standard API key to that API; capture complete Blackwell component SKUs and review the instance mapping |
| Lambda account | Not supplied | Dedicated organization key and source rights. Lambda keys allow all API operations; a read-only key is not documented. Review account isolation and provisioning exposure before installation. [Access model](https://docs.lambda.ai/public-cloud/access-security/) |
| Runpod account | Dedicated restricted key saved locally; read-only GraphQL pricing check returned valid B200 and B300 list quotes on September 6, 2026 | Complete collection/publication terms and hosted secret installation before enabling the source. Null deployment sizes remain unknown availability, not confirmed inventory. Separate Runpod API access is disabled. [Key permissions](https://docs.runpod.io/get-started/api-keys), [collector limits](PROVIDERS.md#runpod) |
| Vast account | Not supplied | Dedicated key restricted to offer search; verify its actual permissions and approved data terms. Console defaults are full-access, and the `misc` category also contains write operations; neither is inherently read-only. Review host provenance. [Authentication](https://docs.vast.ai/api-reference/authentication), [permissions](https://docs.vast.ai/api-reference/permissions) |
| Hyperstack account | Collector implemented; no live key supplied | Key for a least-privileged organization member; verify RBAC access to pricebook, flavors and stocks, then reconcile live billing and source rights. Key creation alone does not establish read-only access. [Authentication](https://docs.hyperstack.cloud/docs/api-reference/authentication/), [RBAC](https://docs.hyperstack.cloud/docs/api-reference/permission/) |
| Shadeform account | Collector implemented; currency and live access unverified | Key, written USD billing confirmation, permission for automated compilation and publication, underlying-provider ownership mapping |
| Prime Intellect account | Collector registered but disabled; no key or live verification | Availability → Read key, written retrieval/retention/derivation/redistribution permission, complete B200/B300 bundle reconciliation and upstream ownership mapping; GB200/GB300 hardware and procurement review |
| Remaining providers | Coverage/discovery matrix recorded | Provider price APIs or documented contributor feeds for each supported hardware family |
| Source rights | Oracle-network production derive/redistribution flags unset; separate hosted-demo approval recorded for Oracle, Azure and Verda | Confirm evidence for automated retrieval, raw retention, public per-provider data, the governed benchmark and onchain financial-reference use; identify which operator is covered. [Demo approval](CENTRALIZED_DEMO.md) does not grant other node operators rights or approve the financial-reference oracle. |
| Provider weights | Unset | Select eligible comparable sources, verify economic groups, approve fixed constituent sets per model and effective date |
| Independent node operators | Two deployed persistent identities, both controlled by elizaos-cloudflare | At least three independent organizations; four are needed to retain a greater-than-two-thirds quorum with one unavailable; verify source access and ownership |
| Pyth accepted publisher | Not supplied | Obtain acceptance as benchmark administrator or onboard first-party providers; confirm current Pro ingress |
| Pyth feed IDs | No matching SBX/Blackwell symbol in queried public catalog | Assigned per-provider/model/composite IDs, metadata, minimum publishers and permitted key bindings |
| Pyth signer | Integration tested against official agent locally | Separate protected production key, relayer access, funded infrastructure if required by agreed terms |
| Pyth consumer access | Not supplied | Hermes/Pro API account and appropriate public redistribution terms |
| Onchain verification | Not performed; Base Sepolia selected first, then Base mainnet | Implement the Pro consumer path, confirm the official deployment/version, obtain an approved signer and capped gas budget, and retain actual signed-update receipts and readback. [Chain decision](PYTH_CHAIN_SELECTION.md) |
| Recurring Pyth readback software | Authenticated Pro monitor and protected operator command implemented locally; exact-release acceptance pending | Complete the [readback monitor](PYTH_READBACK.md) checks against assigned live feeds, credentials and genuine retained snapshots. Configure an owned supervisor and paging. Offchain HTTPS observations do not prove publisher attribution or signed/onchain verification. |
| Target-chain publication service | Base first; Solana and Robinhood Chain planned. Approved feed credentials not supplied | Implement and test Base Pro signed-payload verification and consumer readback first. Add separate Solana runtime tests and Robinhood EVM deployment/version tests. Retain transaction receipts; never count the offchain monitor as chain acceptance. [Implementation gates](PYTH_CHAIN_SELECTION.md), [official contract comparison](https://docs.pyth.network/price-feeds/core/upgrade/contracts) |
| Pyth publishing-node recovery | Implemented locally; exact-release acceptance pending | [Versioned state recovery](PYTH_RECOVERY.md) preserves attempted/queued high-water marks and retained queue receipts, clears restored process locks and rejects unsupported schemas or inherited high-water rollback. Complete exact-release tests and an authorized publishing-node recovery drill. Collector-only evidence does not establish live Pyth operation. |
| Publication scope | Existing runtime requires a publishable SBX composite, including all four model feeds | Meet the all-four-model gate. A one-model pilot requires a reviewed methodology/publication-policy change, matching Pyth approvals and tests; obtaining one feed key does not enable that pilot. [Current policy](../src/pyth/runtime.ts), [composite calculation](../src/engine.ts) |
| Production operations | V2 full-period fixture passed locally and in exact-revision CI; both live journals restored with 103 snapshots reproduced per node | Maintain [exact-release acceptance](HOSTED_ACCEPTANCE_2026-09-06.md); confirm account storage/CPU limits; assign offsite destination, separate key custodian, paging and retention owners; run a host-loss/rotation exercise |
| Restored-node ongoing backups | `backup-stream` exports a read-only consistent copy, preserves prior recovery receipts and independently inspects the encrypted result; V1 still rejects chunked storage | Test the current recovered identity's backup/restore, establish offsite custody and a recurring backup schedule, then document the activation review |
| Historical validation | Retained-data and bounded streaming studies implemented; 30-day qualification `NOT_ESTABLISHED` | Accumulate and review genuine consecutive coverage; obtain licensed historical records; no synthetic backfill or future membership look-ahead; capacity fixtures do not count as real history |
| External audit and benchmark review | Internal independent code review and tests | Security assessment, methodology validation, legal/data-rights review and signed release decision |

Protected entry through `credentials`, environment variables, organization secret storage or deployment secret commands is preferred. If keys are supplied through chat, use dedicated revocable provider keys; never repeat their values in reports, issues, logs or the repository. Wallet recovery phrases and AWS root credentials are not needed and should not be requested. Reference Pyth admission and provider agreements through protected evidence records; private contracts do not belong in the public repository.

## Immediate operator handoff

1. Name the legal benchmark operator, Pyth contact and authorized source-rights reviewer. Provide protected references to existing agreements, if any.
2. Identify the organization secret manager or sign in to the relevant provider dashboards. The current Worker has no provider secrets configured. Start with AWS, Google and the providers where an account already exists; a new paid subscription is not automatically required or authorized.
3. Confirm who will run independently administered nodes. Two nodes under this account are one operator, not two votes.
4. Choose the offsite backup destination, separate key custodian and paging owner. [Hosted streaming recovery](STREAMING_RECOVERY.md) passed exact-release capacity and live restore checks for its documented workload. Ongoing backups for an activated restored node and a real host-loss exercise remain separate requirements.

## Immediate credentials

Start with existing organization accounts. The order below prioritizes restricted access and direct-source coverage; it is not a requirement to open eight accounts. Oracle, Azure and Verda public collection needs no keys. No GPU purchase, subscription, wallet deposit or account creation is implied.

| Priority | Key page | Exact configuration | Additional requirement |
| --- | --- | --- | --- |
| First | [AWS IAM](https://console.aws.amazon.com/iam/) | `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`; `AWS_SESSION_TOKEN` for temporary credentials | `pricing:GetProducts` only for the implemented collector. Temporary credentials need an operational refresh plan; the adapter does not refresh them. [IAM key setup](https://docs.aws.amazon.com/IAM/latest/UserGuide/access-keys-admin-managed.html), [pricing permissions](https://docs.aws.amazon.com/awsaccountbilling/latest/aboutv2/billing-example-policies.html) |
| First | [Google credentials](https://console.cloud.google.com/apis/credentials) | `GOOGLE_CLOUD_BILLING_API_KEY` | Enable Cloud Billing API and restrict the key to it. Supply reviewed `GOOGLE_BILLING_SKU_MAP_JSON` after catalog inspection; a key alone returns discovery, not composed instance prices. No service-account key or billing-account IAM access is needed for the public catalog. [Catalog setup](https://docs.cloud.google.com/billing/v1/how-tos/catalog-api), [permissions](https://docs.cloud.google.com/billing/docs/access-control) |
| First | [Runpod settings](https://www.console.runpod.io/user/settings) | `RUNPOD_API_KEY` | New restricted/read-only access as described above; confirm the implemented Secure Cloud pricing query succeeds. |
| Next | [Lambda keys](https://cloud.lambda.ai/user/api-keys) | `LAMBDA_API_KEY` | Explicitly review full API/provisioning access; do not label this a read-only key. |
| Next | [Hyperstack keys](https://console.hyperstack.cloud/api-keys) | `HYPERSTACK_API_KEY` | Verify the member's effective RBAC permissions and live pricebook/flavor/stock joins. |
| Next | [Vast keys](https://cloud.vast.ai/manage-keys/) | `VAST_API_KEY` | Restrict and verify offer-search access; do not grant the entire `misc` category on the assumption it is read-only. |
| Research | [Prime Intellect keys](https://app.primeintellect.ai/dashboard/tokens) | `PRIME_INTELLECT_API_KEY` | Availability → Read with expiry. B200/B300 account-specific quotes remain outside the public-list cohort; GB200/GB300 remain discovery-only. [Key setup](https://docs.primeintellect.ai/api-reference/api-keys), [required scope](https://docs.primeintellect.ai/api-reference/check-gpu-availability) |
| Research | [Shadeform keys](https://platform.shadeform.ai/settings/api) | `SHADEFORM_API_KEY` | Also require `SHADEFORM_BILLING_CURRENCY=USD` and a reviewed `SHADEFORM_BILLING_EVIDENCE` reference. Read-only key scope is unverified. The guide specifies wallet funding before key creation; seek existing or catalog-only access before considering an explicitly approved deposit. [Quickstart](https://docs.shadeform.ai/getting-started/quickstart) |

Each new source still needs live schema, hardware, unit and commercial-term verification plus documented usage rights. Keys do not satisfy independent-operator admission, approved weights or Pyth onboarding. Authenticated account-specific prices must not be relabeled as public list prices.

Use the [operating study](OPERATING_STUDY.md) to inspect retained private observations and dated gaps. It does not establish 30 consecutive days of qualified operation or authorize publication.

The capacity release must pass the complete [streaming recovery plan](STREAMING_RECOVERY_PLAN.md), including local inspection/restore, full-period study support and actual-runtime acceptance. Raising export limits or completing only the download path is insufficient. A three-operator capacity result does not establish retention at the registry's maximum operator count.

Continue research collection while these are unresolved. Do not enable public prices, Pyth submission or a financial-reference claim merely to make the dashboard look complete.

## Pyth onboarding packet to prepare

Supply the legal operator, contact, data provenance, provider permissions, requested symbols, units, exponent, refresh schedule, source-time semantics, constituent methodology, quorum model, expected coverage, failure behavior, test evidence and public publisher key. Request a written decision on benchmark-administrator admission, approved feeds, minimum independent publishers, test/production ingress and fees. Ask how the upgraded Core/Pro API exposes source age and how to obtain independent publisher-attribution evidence.

These are drafted requirements, not a sent application or a claim of acceptance. Contacting external parties requires the user's communication authorization.
