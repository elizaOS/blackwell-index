# Development deployment — 6 September 2026

This record concerns a real-data collection deployment. It is not acceptance of a public price benchmark or a Pyth feed.

## Published release

- Repository: [elizaOS/blackwell-index](https://github.com/elizaOS/blackwell-index), public, MIT licensed.
- Initial source revision: `f53368bf044a44e044841215d7280ed1535886fc`.
- Initial Worker version: `3ec983d8-15ed-4981-8830-202b45c81bf4`.
- Initial hosted verification: [release workflow](https://github.com/elizaOS/blackwell-index/actions/runs/34023043630), both jobs completed successfully at 08:55 UTC.
- Initial production readback: 08:52–08:53 UTC, 6 September 2026.

## Live results

| Check | Result |
| --- | --- |
| Index | `https://blackwellindex.com/` returned 200 with the index title |
| ALTX | `https://altx.exchange/` returned 200 with its distinct landing page |
| Aliases | `blackwell.fyi` and `blackwell.today` returned 308 to the canonical index |
| DNS and HTTPS | All six configured hostnames resolved and passed HTTPS verification; the four domain A records were additionally read from external resolvers |
| Served source | Both HTML pages, the price UI script and stylesheet matched the committed local bytes by SHA-256 |
| Primary node | `7d9c8715f4d0672e891579113ba597aaddb48fe0ac25456d84051a6ed9be47e4` |
| Secondary node | `0ba8abd23b527db13769ccb0e4548f2cdab1bb01465fdea2f89cf68ff0c1e010` |
| Ownership | Both report `elizaos-cloudflare`; one operator group, not two independent operators |
| First collection, each node | 42 real observations: Oracle 4, Azure 38; all four hardware families; zero source errors; five retained response bodies |
| Publication | Zero shared observations, zero admitted public reports, all 33 configured feed values null, readiness HTTP 503 |
| Pyth | `NOT_PUBLISHED`; no production publisher key, assigned SBX feed or upstream publication evidence |
| Revision readback | Both node status responses reported the initial source revision above |
| Internal routes | Internal wake and unknown-node paths returned 404 |

These source counts describe one capture, not a fixed coverage guarantee. A successful HTTPS request verifies certificate acceptance at that time, not future renewal or DNSSEC configuration. Source observations are retained privately until the appropriate permissions exist.

## Test evidence

The initial complete local verification passed 96 tests with 431 assertions, including the actual official Rust Pyth agent integration. Three protected-credential regressions were then added. Hosted Linux verification passed 98 tests with 455 assertions and skipped only the separately executed Rust integration. The separate official-agent job passed all 14 Pyth tests with 74 assertions, including that integration. Type checking, the production image build and the container setup/restart/reproduction smoke test all passed.

Final local verification after adding the deployment checker passed all 99 tests with 464 assertions, with the real Pyth agent test enabled and no skips. The follow-up workflow pins current Node 24 checkout/cache releases to exact commits; inspect the latest workflow for the final documentation/checker revision as well as the initial release run above.

Local workerd restart checks preserved two distinct identities, signed sequence state, private evidence and collection alarms. Both production nodes completed a second scheduled collection at 08:57 UTC: 42 real observations each, zero source errors, unchanged identities and increasing capture/snapshot counters. Desktop and mobile browser checks passed the index, methodology and ALTX navigation without page overflow or site-origin JavaScript errors. None of these checks replaces a backup restoration exercise, an external security review, or genuine historical price qualification.

The read-only `scripts/verify-deployment.ts` check passed 52 live assertions, including six exact asset hashes, both node identities/revisions, current real collections, deliberately null public feeds, alias path/query preservation and 40 denied private routes. It also rejected invalid and mismatched release identifiers.

One browser limitation remains: the existing Chrome profile reported `ERR_BLOCKED_BY_CLIENT` on direct navigation to `/v1/feeds`, while the same page's JavaScript fetch and independent HTTPS requests succeeded. The cause was not established. No alternative managed browser was available, and isolated local headless startup did not produce a usable result. No security headers or user browser settings were weakened. A clean-browser direct JSON navigation check remains open; it is separate from the successful API and dashboard checks.

## Follow-up deployment acceptance

Revision `cbe7f00812b0ee252cbc98d9c1e4b7c8af68b4f5` deployed as Worker version `363736d9-18d6-40c0-949e-697ae5a2339e`. Both jobs in its [exact-revision workflow](https://github.com/elizaOS/blackwell-index/actions/runs/34023592842) completed successfully. At 09:05 UTC the deployed revision passed all 52 live checks again. Both original identities survived the ordinary redeployment, with three completed captures/snapshots and five retained source responses each. This verifies persistence across that deployment, not backup restoration.

The runtime and assets were unchanged in this follow-up; it added release evidence, the acceptance checker and pinned current CI actions. Subsequent documentation-only revisions can identify themselves through `hosting.release`; run the same verifier against the actually deployed commit. Keep deployment metadata and checklist completion separate from unresolved benchmark/Pyth admission.

## Remaining acceptance

The [launch requirements](LAUNCH_TODO.md) remain binding: source accounts and rights, approved methodology, independent operators, Pyth admission and feed configuration, actual signed upstream/onchain readback, operational recovery and a real operating-history study. No simulated prices or synthetic historical backfill are part of this deployment.
